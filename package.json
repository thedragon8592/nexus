{
  "name": "nexus-chat-server",
  "version": "3.4.0",
  "description": "Real-time social chat and live performance tools for Survev and Resurviv.",
  "license": "MIT",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=20"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "preview": "node scripts/preview-server.js",
    "check": "node --check server.js && node --check public/client.js && node --check public/nexus-chat.user.js && node --check public/nexus-optimizer.user.js",
    "build:extension": "node scripts/build-extension.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@libsql/client": "^0.17.4",
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "socket.io-client": "^4.7.2"
  }
}

{
  "version": "3.5.0",
  "changes": [
    "Persistent unread counters for direct messages received while offline",
    "Friend conversations sorted by their most recent message",
    "Direct-message read state saved in Turso across sessions",
    "Lower-overhead rendering, sounds, scrolling, and startup loading",
    "Client-side player rotation remains enabled in optimized presets"
  ],
  "bugs": [
    "Fixed offline messages remaining invisible until every friend chat was opened",
    "Removed the animated kill-leader GIF, polling, and observer overhead",
    "Avoided repeated scroll animations while rebuilding message history",
    "Rebuilt the extension as a compatible ZIP without invalid relative paths",
    "Synchronized the web client, userscript, and browser extension"
  ]
}
